--C - creating data
--R - reading data
--U - updating data
--D - deleting data

--C - CREATE, INSERT
--R - SELECT
--U - UPDATE
--D - DELETE, DROP

--flat-file databases are files like .csv and .tsv(tab-separated values).

--JOIN and implicit JOIN;
--SELECT column1 FROM table1 JOIN table2 ON table1.column1 = table2.column2
--SELECT column1, column2 FROM table1, table2 WHERE table1.column1 = table2.column2(ensures the foreign key lines up)

--CREATE INDEX (anyname) ON table(column);
--This creates a data struct B-Tree, which increase the search a lot faster.

--Race Condition; BEGIN TRANSACTION, COMMIT, ROLLBACK;

--SQL Injection Attack - Do not use f-string when using SQL query. Use the library's implemented plaace holder(malicious charaters like single-quote are escaped.)

--Same like regex, using SQL in Python will go through two parser, Python interprets the str first before passing to SQL.